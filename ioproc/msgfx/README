This is the microsoft graphics library package (msgfx)

It has an INT 60 handler (msg) that deals with the graphics at the PC end.
a Helios library that implements the microsoft graphics std library interface at
the Helios end (xp). This library interface communicates through the IO Servers
INT 60 interface to the handler that then calls the microsoft graphics library.
