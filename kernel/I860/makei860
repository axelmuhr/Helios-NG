# Make Helios/TRAN on the orion

setenv HPROC I860
setenv HHOST PC

set hsrc=/d/helios
setenv HSRC  $hsrc

setenv HPROD /c/i860/helios

setenv HHOSTBIN /c/helios/bin

#if (-e I860) then
#	cd I860; make $*
#else
	make  $* 
#endif
