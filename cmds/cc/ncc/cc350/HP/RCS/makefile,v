head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	92.07.01.11.01.43;	author nickc;	state Exp;
branches;
next	;


desc
@makefile for the SNAKE hosted version of the C40 C compiler
@


1.1
log
@Initial revision
@
text
@default:
	@@ cd .. ; unset MAKEFLAGS ; make ccc40

all:
	@@ cd .. ; unset MAKEFLAGS ; make all

clean:
	@@ cd .. ; unset MAKEFLAGS ; make clean-HP

install:
	@@ cd .. ; unset MAKEFLAGS ; make install-HP

@
